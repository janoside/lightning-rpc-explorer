table(class="table table-responsive-sm")
	thead
		tr
			if (showTableNumbers)
				th
			th Node
			th Address
			th Last Update
			th(class="text-right") Channels
			th(class="text-right") Total Capacity
			
	
	tbody
		each node, index in nodes
			include ./node-details-table-row.pug